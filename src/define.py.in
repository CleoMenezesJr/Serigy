# Copyright 2024-2025 Cleo Menezes Jr.
# SPDX-License-Identifier: GPL-3.0-or-later

from pathlib import Path

import gi

gi.require_version("GdkPixbuf", "2.0")

if gi:
    from gi.repository import GdkPixbuf, GLib

APP_ID = "@APP_ID@"
APP_PATH = "@PREFIX@"
VERSION = "@VERSION@"
PROFILE = "@PROFILE@"
RESOURCE_PATH = "/io/github/cleomenezesjr/Serigy"

# if PROFILE == "development":
#     APP_PATH = "/io/github/cleomenezesjr/Serigy/Devel"

cache_dir = Path(GLib.get_user_cache_dir())
log_files: list = []

supported_image_formats: list = [
    format.get_mime_types()[0] for format in GdkPixbuf.Pixbuf.get_formats()
]
supported_file_formats: list = [
    "GdkFileList",
    "GFile",
    "application/vnd.portal.files",
    "application/vnd.portal.filetransfer",
    "x-special/gnome-copied-files",
]
supported_text_formats: list = [
    "gchararray",
    "text/plain",
    "text/plain;charset=utf-8",
    "text/html",
    "UTF8_STRING",
    "STRING",
    "TEXT",
]
